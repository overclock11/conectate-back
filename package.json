{
  "name": "conectate",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "rm -rf frontend/conectate-front || true && rm -rf node_modules || true",
    "postinstall": "./deploy-fe.sh",
    "fe-dev": "cd frontend/conectate-front && ng build --watch --output-path ../public"
  },
  "private": true,
  "devDependencies": {
    "@angular/cli": "~1.7.3",
    "@angular/compiler-cli": "^5.2.9",
    "@angular/language-service": "^5.2.9",
    "typescript": "~2.5.3"
  },
  "engines": {
    "node": "9.x"
  }
}
